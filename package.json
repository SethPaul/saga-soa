{
  "name": "saga-soa",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "test": "pnpm -r --if-present test",
    "test:ci": "pnpm exec nx test api --coverage --watchAll=false",
    "test:e2e": "pnpm exec nx test api --watchAll=false",
    "clean": "pnpm -r --if-present clean",
    "dep:list": "pnpm syncpack list",
    "dep:fix": "pnpm syncpack fix",
    "ci:lint": "pnpm exec nx run-many -t lint --parallel=3",
    "ci:test": "pnpm exec nx test api --coverage --watchAll=false",
    "ci:build": "pnpm exec nx run-many -t build --parallel=3",
    "ci:check-types": "pnpm exec nx run-many -t check-types --parallel=3",
    "docker:build": "docker build -f apps/api/Dockerfile -t saga-soa-api .",
    "docker:run": "docker run -p 3000:3000 saga-soa-api",
    "security:audit": "pnpm audit --audit-level moderate",
    "prepare": "pnpm install --frozen-lockfile"
  },
  "devDependencies": {
    "@nestjs/schematics": "^11.0.0",
    "@nestjs/testing": "^11.0.0",
    "@nx/jest": "21.2.1",
    "@nx/nest": "21.2.1",
    "@types/figlet": "^1.5.8",
    "@types/jest": "^29.4.0",
    "@types/supertest": "^2.0.12",
    "jest": "^29.4.0",
    "nx": "21.2.1",
    "prettier": "^3.5.3",
    "supertest": "^6.3.3",
    "syncpack": "14.0.0-alpha.16",
    "ts-jest": "^29.1.0",
    "turbo": "^2.5.4",
    "typescript": "5.8.2"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  },
  "version": "1.0.0",
  "dependencies": {
    "@nestjs/common": "^11.0.0",
    "@nestjs/config": "^3.0.0",
    "@nestjs/core": "^11.0.0",
    "@nestjs/platform-express": "^11.0.0",
    "@swc/helpers": "^0.5.17",
    "bunchee": "^6.5.4",
    "figlet": "^1.7.0",
    "mongodb": "^5.7.0",
    "reflect-metadata": "^0.2.0",
    "rxjs": "^7.8.1",
    "zod": "^3.22.2"
  }
}
